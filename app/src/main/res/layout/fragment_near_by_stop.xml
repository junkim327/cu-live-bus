<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/coordinator_layout_near_by_stop"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/toolbar_near_by_stop"
        layout="@layout/toolbar_small_size_transparent" />

    <fragment
        android:id="@+id/map_near_by_stop"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <include
        android:id="@+id/bottom_sheet_near_by_stop"
        layout="@layout/bottom_sheet_bus_stop_info" />

</android.support.design.widget.CoordinatorLayout>